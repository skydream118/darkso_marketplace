import { NFT } from '../../modules/dashboard/types'

export type Props = {
  asset: NFT
  // className?: string
  // isDraggable?: boolean
  // withNavigation?: boolean
  // hasPopup?: boolean
  // zoom?: number
  // isSmall?: boolean
  // showMonospace?: boolean
}
